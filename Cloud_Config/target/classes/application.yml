server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sriharish16/Cloud_Config_2022.git
          search-paths:
            - '{application}'
          
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"